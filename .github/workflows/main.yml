name: Deploy to linux
on: push
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using ssh key
      script: |
        ssh gavin@192.168.1.121
        ls
